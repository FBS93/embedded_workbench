# Generate ebf_config.h based on ebf_config.h.in
configure_file(ebf_config.h.in ${CMAKE_CURRENT_BINARY_DIR}/inc/ebf_config.h @ONLY)

add_library(ebf STATIC "")
target_include_directories(ebf
    PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/inc
    PUBLIC ${CMAKE_CURRENT_BINARY_DIR}/inc) # Required to find autogenerated ebf_config.h file
add_subdirectory(src)
