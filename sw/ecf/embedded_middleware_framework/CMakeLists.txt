# Generate emf_config.h based on emf_config.h.in
configure_file(
  emf_config.h.in
  ${CMAKE_CURRENT_BINARY_DIR}/inc/emf_config.h
  @ONLY
)

add_library(emf STATIC "")
target_include_directories(
  emf
  PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}/inc
    ${CMAKE_CURRENT_BINARY_DIR}/inc # Required to find autogenerated emf_config.h file
)
add_subdirectory(src)
